# 微信小程序交互改造设计

- 日期：2026-06-14
- 项目目录：`/Users/husen/Documents/高考专业填报`
- 目标对象：`miniapp`
- 状态：待用户确认后实施

---

## 1. 改造目标

把当前 `miniapp` 从“网页缩小版”改成更像真实微信小程序的操作节奏和视觉语言。

核心目标：

1. 首页更像微信工具入口，而不是介绍页
2. 问卷更像单题选择器，而不是表单
3. 报告更像结论卡片流，而不是长报告
4. 全局节奏更轻、更快、更适合手机单手操作

---

## 2. 用户已确认的设计决策

### 2.1 首页第一屏

优先强调：

`马上开始诊断`

而不是：

1. 长说明
2. 家长协作说明前置
3. 数据来源前置

### 2.2 问卷节奏

采用：

`一次只看一题，选完自动进入下一题`

说明：

1. 单选题点选后自动切题
2. 多选题保留“继续”按钮
3. 尽量减少“下一步”按钮存在感

### 2.3 报告首屏

优先展示：

`类型标签 + 为什么会这样`

而不是：

1. 直接先上方向排序
2. 先上避坑提醒

---

## 3. 产品策略

本次改造采用：

`微信实用工具骨架`
-> `强结果表达`

不采用：

1. 聊天顾问式页面
2. 强游戏化投票器
3. 网页大 Hero 介绍式布局

设计原则：

1. 更像微信里的顺手工具
2. 更少解释，更多直接动作
3. 更短文案，更强层级
4. 更少按钮，更快推进

---

## 4. 页面结构设计

### 4.1 首页

目标：

`先让用户开始`
-> `再补充理解`

页面结构：

1. 顶部轻标题区
   - 小标题：工具属性
   - 主标题：直接点题
   - 一句副标题：强调 3 分钟和缩小范围

2. 中间信任卡
   - 只保留 2 到 3 条非常短的说明
   - 不做大段项目介绍

3. 主操作区
   - 主按钮：`开始 3 分钟诊断`
   - 次按钮：`查看说明`

4. 底部轻提示
   - 一句合规提示
   - 一句适用人群说明

### 4.2 问卷页

目标：

`看一眼`
-> `点一下`
-> `进入下一题`

页面结构：

1. 顶部极简进度
   - 当前题号
   - 完成百分比
   - 细进度条

2. 中部单题卡
   - 题目
   - 一句短说明
   - 选项列表

3. 选项逻辑
   - 单选：点完自动下一题
   - 多选：满足最少数量后激活底部继续按钮

4. 底部操作区
   - 默认不堆多个按钮
   - 保留轻量返回
   - 多选题显示主按钮 `继续`

### 4.3 报告页

目标：

`先认领自己`
-> `再接受建议`

页面结构：

1. 首屏结果卡
   - 类型标签
   - 一句话判断
   - 为什么会这样

2. 方向判断卡
   - 现在优先押
   - 可以保留
   - 先别碰

3. 现实层提醒卡
   - 市场现实结论短句化
   - 不做长段资料堆叠

4. 行动清单卡
   - 只保留 2 到 3 条
   - 必须能马上执行

---

## 5. 视觉语言

整体风格：

`浅底`
-> `轻卡片`
-> `单主按钮`
-> `中等密度信息`

具体规则：

1. 页面背景使用浅灰蓝或浅米白
2. 卡片边框轻、阴影淡，不靠厚重阴影制造层级
3. 标题更紧凑，正文更短
4. 主按钮统一深色高对比
5. 次按钮弱化，不与主按钮抢注意力
6. 风险信息用暖色区分

---

## 6. 交互动效

### 6.1 问卷

1. 单选后轻微高亮
2. 150ms 到 250ms 自动切题
3. 进度条同步推进
4. 切题只做轻微位移和淡入

### 6.2 报告

1. 首屏结论卡完整露出
2. 后续卡片自然纵向流动
3. 重点通过标题、关键词、风险色突出
4. 不做复杂转场

---

## 7. 实施范围

### P0 本次必须改

1. `miniapp/src/pages/home/index.tsx`
2. `miniapp/src/pages/home/index.scss`
3. `miniapp/src/pages/questionnaire/index.tsx`
4. `miniapp/src/pages/questionnaire/index.scss`
5. `miniapp/src/pages/report/index.tsx`
6. `miniapp/src/pages/report/index.scss`
7. `miniapp/src/app.scss`

### P0 本次不改

1. 评分规则
2. 报告算法
3. 市场数据结构
4. 多人协作逻辑
5. 分享链路
6. 外部数据接入

---

## 8. 验收标准

改造完成后，至少满足以下标准：

1. 首页首屏主要注意力集中在“开始诊断”
2. 单选题不需要手动点“下一步”也能推进
3. 大多数主流手机下，单题页不应像网页表单一样显得拖沓
4. 报告首屏先看到“类型标签 + 为什么会这样”
5. 整体视觉更接近微信工具，而不是网页缩小版
6. `miniapp` 仍然可以通过 `npm run typecheck` 和 `npm run build:weapp`

---

## 9. 风险与注意事项

1. 自动切题必须控制节奏，不能快到用户误触后无感
2. 多选题必须明确最低选择数量，否则用户会困惑
3. 首页不能因为“工具化”而丢掉基本信任感
4. 报告页虽然要短，但不能短到失去判断依据
5. 视觉“小程序化”不等于做成模板化蓝白后台工具

---

## 10. 实施建议

推荐实现顺序：

1. 先改全局样式基调
2. 再改首页结构
3. 再改问卷单题节奏
4. 最后改报告首屏和卡片流
